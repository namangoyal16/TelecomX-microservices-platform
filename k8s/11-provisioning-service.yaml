apiVersion: apps/v1
kind: Deployment
metadata:
  name: provisioning-service
  namespace: telecomx
spec:
  replicas: 2
  selector: { matchLabels: { app: provisioning-service } }
  template:
    metadata: { labels: { app: provisioning-service } }
    spec:
      containers:
        - name: provisioning-service
          image: ghcr.io/your-org/telecomx-provisioning-service:latest
          ports: [{ containerPort: 8082 }]
          envFrom:
            - configMapRef: { name: telecomx-shared-config }
          env:
            - { name: SPRING_DATASOURCE_URL, valueFrom: { secretKeyRef: { name: telecomx-secrets, key: PROVISIONING_DB_URL } } }
            - { name: SPRING_DATASOURCE_USERNAME, valueFrom: { secretKeyRef: { name: telecomx-secrets, key: POSTGRES_USER } } }
            - { name: SPRING_DATASOURCE_PASSWORD, valueFrom: { secretKeyRef: { name: telecomx-secrets, key: POSTGRES_PASSWORD } } }
            - { name: JWT_SECRET, valueFrom: { secretKeyRef: { name: telecomx-secrets, key: JWT_SECRET } } }
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8082 }
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8082 }
            initialDelaySeconds: 30
            periodSeconds: 15
          resources:
            requests: { cpu: "150m", memory: "384Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata: { name: provisioning-service, namespace: telecomx }
spec:
  selector: { app: provisioning-service }
  ports: [{ port: 8082, targetPort: 8082 }]
